{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "HOST_IP=$(boot2docker ip) mocha ./test_*.js"
  },
  "dependencies": {
    "jsonwebtoken": "^4.2.2",
    "mocha": "^2.2.1",
    "super-request": "0.0.8"
  }
}
